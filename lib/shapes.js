//Creates the base for the Shapes
class Shapes {
    constructor(shapeColor, text, textColor) {
        this.shapeColor = shapeColor;
        this.text = text;
        this.textColor = textColor;
    }
    // function remains to be filled for the helper Classes
    svgRender() {
    
    }
}

module.exports = Shapes;